namespace PolyLab3DStudio.Core;

/// <summary>A ribbon transform tool (select / move / rotate / scale).</summary>
public sealed record ToolDefinition(string Id, string Label, string English, string Key, string IconData);
