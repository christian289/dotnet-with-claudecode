namespace PolyLab3DStudio.ViewModels;

/// <summary>A small tool card in the point cloud section (CloudCompare, MeshLab).</summary>
public sealed record MiniToolCard(string Name, string Body, string Badge);
